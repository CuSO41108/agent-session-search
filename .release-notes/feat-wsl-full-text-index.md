# WSL 会话支持后台全文索引

## 新增功能

- WSL 中的 Codex 和 Claude Code 会话会在后台逐步建立完整消息索引，搜索正文、匹配片段和定位消息更加完整。
- WSL 会话在后台索引后仍保留工具调用和执行过程详情。
- 会话搜索会优先展示完整短语命中，其次展示包含全部关键词的消息；点击结果可直接定位到优先命中的消息。
- 大体量 WSL 会话也能正常完成搜索索引，不再因为内容过大而无法建立索引。
