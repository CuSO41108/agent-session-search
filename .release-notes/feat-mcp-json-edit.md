# MCP Server 支持以 JSON 方式编辑

## 新增功能

- MCP 页面新增「JSON」按钮：自建的 MCP Server 可以直接以 JSON 查看和编辑当前连接配置（stdio 的命令/参数/环境变量引用，HTTP 的地址/请求头引用，以及按工具停用列表），改完点「应用」再「保存」即可生效。
- JSON 编辑会原样保留环境变量与请求头的宿主机变量名引用，密钥值依旧不会写入数据库；粘贴整段 `mcpServers` 批量配置时会提示改用「从 JSON 导入」。
