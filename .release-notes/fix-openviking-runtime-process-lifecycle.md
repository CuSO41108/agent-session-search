# 修复 OpenViking 运行时进程生命周期管理

## Bug 修复

- 🧟 修复 OpenViking 运行时进程退出后 `once("exit")` 监听器未清理导致的内存泄漏，长时间运行的会话不再泄漏 `start()` 闭包。
- 修复僵尸进程被误判为运行中的问题：原先仅通过 `process.kill(pid, 0)` 检测进程存活，但该方法对僵尸进程也返回成功，现补充系统进程状态检查，防止将已死亡的僵尸进程当作正常运行。

<!-- release-target: v2 -->