# 直接安装即可使用 v2

<!-- release-target: v2 -->

## 新增功能

- ✨ 现在可以像 v1 一样直接安装 AgentRecall v2，不再需要克隆仓库并从源码启动。安装后在任意终端运行 `agent-recall-v2` 即可打开应用。
- ✨ v2 提供固定的安装链接，覆盖安装和回滚都不用再查具体版本号；自动更新失败时给出的手动安装命令也始终指向最新版本。
