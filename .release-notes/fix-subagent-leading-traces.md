# 修复子代理会话轮次显示

<!-- release-target: both -->

## Bug 修复

- 子 Agent 列表会稳定标明由父会话的哪一轮发起；在 2.0 预览中，有明确任务起点时会话会区分从父会话 Fork 的上下文、会话准备步骤和子 Agent 自己执行的轮次，没有任务起点时不会强行分区；无用户输入的 Agent 任务及其最终回复也会按真实轮次展示，不再出现“第 0 轮”、重复轮号、多条“前置轨迹”或因细微时间差混入继承的父轮次。
